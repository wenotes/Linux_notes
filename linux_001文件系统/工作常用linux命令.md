
```


netstat -anp |grep 端口号

kill -9 进程id

ps -ef| grep '模糊查询进程'

tail -100f 日志文件

> 目标文件 

du -s -m * 

scp -r 目录路径 目标用户@目标机器地址:目标路径





编辑文件，底行模式：
    :/set file=unix  用于把Windows文件转为linux文件

编辑文件，命令行模式：
    gg          到首行
    shift + g   到尾行

```
